---
name: public-ready
description: 扫描网站/Web 应用中的「生硬」「技术向」「未完成」「硬编码」文案，产出一张对账表（audit table），按四类列出：🔴 不适合直接上线（占位/测试/调试残留）、🟠 用户友好向（技术词、公文腔、术语外露）、🟡 与初衷不符（语气/口径与产品定位矛盾）、⚪ 硬编码项（写死的 URL/账号/版本号/重复文案）。当用户想检查产品文案是否达到上线标准、打磨用户向文案、排查硬编码与占位文案、或提到「文案扫描」「文案审计」「对账表」「上线检查」「文案生硬」「技术向文案」时使用。
version: 1.0.0
author: jry21223
license: MIT
metadata:
  hermes:
    tags:
      - copy-audit
      - launch-readiness
      - ux-copy
      - content-audit
    related_skills:
      - code-review
---

# 文案上线对账扫描（Copy Launch Audit）

扫描用户可见文案，产出「哪里不适合上线」的对账表。**默认只出表、不改代码**；用户明确要求后才动手修改。

## 何时使用

- 上线前检查网站/应用里的用户可见文案是否达标
- 用户觉得文案「生硬」「技术向」「像没做完」
- 需要一份「哪里要改」的清单，而不是直接改代码
- 排查硬编码：写死的域名、测试账号、版本号、提示语、重复文案

## 核心原则

1. **先出对账表，不直接改代码**。对账表是交付物，修改是用户确认后的后续动作。
2. **每条发现必须可溯源**：位置（文件:行号）+ 原文 + 分类 + 问题 + 建议 + 优先级，缺一不可。
3. **判断基于证据**：引用具体文件和原文；拿不准的标注「存疑」并说明推断依据，不瞎编。
4. **尊重产品初衷**：先弄清产品定位、目标用户、语气基调再判断「与初衷不符」；没有文档就向用户确认，或用一句话记录假设并标注。
5. **报错信息必须写清原因**：暴露给用户的报错至少包含「发生了什么 + 用户能做什么」，禁止只甩「请稍后重试」「系统繁忙」；后端透传的原始错误（HTTP 状态、异常堆栈、内部配置问题）必须转换成用户能理解的表述，不得直出。
6. **扫描分派 subagent 执行**：不要让大量文案塞进主上下文。主 agent 只负责定范围、发 subagent、汇总对账表；逐文件读文案的工作全部下放给 subagent，防止上下文污染影响分类判断。

## 工作流程

### 第 1 步：确定范围与初衷

- 确认扫描范围：整个前端 / 某个页面 / 某个模块；用户没指定就默认扫整个项目里的用户可见文案
- 找初衷：README、PRD、产品文档、领域文档（如 `docs/` 下的词汇表/上下文说明）、设计稿里的文案规范、历史文案
- 找不到就问一句：这产品给谁用、想传达什么感觉？把答案用一句话记在报告开头，作为 🟡 类的判断基准

### 第 2 步：扫描用户可见文案（必须分派 subagent）

用户可见文案 = 最终渲染到页面上的字符串。扫这些来源：

- UI 模板与组件：`.html` `.jsx` `.tsx` `.vue` `.svelte` 里的文本节点、`title`、`placeholder`、`aria-label`、`alt`、按钮、toast/提示语、空状态
- 国际化文件：`i18n`、`locale`、`messages`、语言包 `.json`
- 后端错误信息中会透传到前端的文案
- 配置与常量：路由、菜单、footer、联系信息、版本号、环境配置
- 静态示例数据：写死的榜单、示例订单、演示卡片——确认是展示稿还是真实数据，防止「示例当真实」

**分派方式**：按用户可见面拆成多个并行 subagent（如 web 端、管理端、移动端、共享 UI 包各一个；具体目录以目标项目实际结构为准），每个 subagent 的提示词必须包含：

```
你是文案审计扫描员。
扫描范围：<目录>（只扫这个目录）。
产品初衷：<一句话，来自第 1 步>。
用户可见文案 = 最终渲染到页面上的字符串（文本节点、placeholder、aria-label、alt、按钮、toast、空状态、i18n 语言包、后端透传错误文案、写死的示例数据）。
判断标准：先读特征库（见本提示词末尾“特征库内容”），按 A/B/C/D 四类核对；报错文案检查「发生了什么 + 用户能做什么」是否齐全。
grep 线索命中不代表有问题，只有用户可见的才算发现；基础设施/测试代码不算。
输出：每条发现一行，格式「位置(文件:行号) | 原文(截断 60 字) | 分类(可组合如 A+D) | 问题 | 建议 | 优先级(🔴/🟠/🟡/⚪)」。只输出命中项，不要解释说明。

[特征库内容：将 references/stiff-copy-patterns.md 全文粘贴在此]
```

**可移植性要求（发布型 skill 强制）**：提示词中禁止出现写死的绝对路径或本机特定信息。特征库的传递方式二选一：

1. **内联全文（推荐）**：主 agent 把 `references/stiff-copy-patterns.md`（与本 SKILL.md 同目录，用相对路径读取）全文粘贴进 subagent 提示词末尾——零路径依赖，任何安装位置都可用；
2. 或写 `{SKILL_DIR}/references/stiff-copy-patterns.md` 占位符，发送前用 skill 信封 `<directory>` 提供的实际目录替换，并要求 subagent 自行读取。

主 agent 只做：拆分范围 → 收集 subagent 返回的命中项 → 汇总对账表。subagent 返回内容过大时要求其只回命中项。**主 agent 自己不要逐文件读页面文案**（读少量文件确认存疑项除外）。

### 第 3 步：分类打标

| 分类 | 含义 | 典型特征 |
|------|------|----------|
| 🔴 A 不适合直接上线 | 未完成、占位、调试残留、暴露内部信息 | `TODO`、`lorem`、`测试`、`示例`、`XXX`、`localhost`、报错堆栈直出、`undefined` 渲染到页面 |
| 🟠 B 用户友好向 | 技术词外露、生硬书面语/公文腔、甩锅式提示、语气不一致 | 「参数校验失败」「接口超时」「token 过期」「404」「系统繁忙请稍后重试」 |
| 🟡 C 与初衷不符 | 语气/口径与产品定位矛盾、价值主张缺失 | 校园正式平台用「兄弟们冲鸭」；学生工具用「贵司予以回复」；首页只说「XX 系统 v1.0」不讲价值 |
| ⚪ D 硬编码项 | 写死的、应配置化/走 i18n 的内容 | 硬编码 URL/IP/端口、环境名（test/dev）、版本号、联系人、日期、重复出现 3 次以上的同一文案 |

一条发现可同时打多个分类，用 `+` 连接（如 `A+D`），主分类在前。空状态缺失、错误页未设计也归入 A。

### 第 4 步：产出对账表

报告结构：

1. **概述**：扫描范围、产品初衷（含假设标注）、发现总数、各分类计数
2. **对账表**：Markdown 表格，按优先级从高到低排
3. **优先级建议**：🔴 上线阻塞（必须改）/ 🟠 上线前应改 / 🟡 建议改 / ⚪ 可分批配置化
4. **改法示例**：每类给 1–2 条具体改法示范

对账表格式：

```
| # | 分类 | 位置 | 原文 | 问题 | 建议 | 优先级 |
|---|------|------|------|------|------|--------|
```

- 位置写「项目相对路径:行号」，同一文件多处用 `:12-15` 或拆多行
- 原文照抄，代码字符串加反引号；长文案截断但保留关键句
- 「问题」写为什么不行（对用户/对上线意味着什么）
- 「建议」写具体改法方向；拿不准时给两个候选让用户选

### 第 5 步：收尾

- 点出最该先处理的 3 个高优先级项，问用户是否动手改
- 用户确认修改后：逐条改，保持全站语气口径一致；**改报错文案时补全「发生了什么 + 用户能做什么」**；改完重跑相关 grep 确认无残留
- 硬编码的敏感信息（密钥、测试账号）即使不在扫描范围，也要单独提醒用户

## Common Pitfalls（常见陷阱）

- **grep 命中 ≠ 发现**：线索只用于定位，必须读上下文确认用户可见；测试、CI、构建配置、服务端内部日志不算发现。
- **主 agent 亲自逐文件读文案**：违背 subagent 机制，会污染上下文影响分类判断；只允许读少量文件核实存疑项。
- **漏查 mock 渲染路径**：mock 数据即使有环境开关保护，也可能作为 store 初始态、SSG 静态页渲染给用户；要追数据流（store 初始值、generateStaticParams、fallback），不能只看开关。
- **把「诚实性文案」误判成问题**：「未使用示例数据填充」这类不伪造数据的说明是正确设计，问题只在表达技术化——标 B 而非 A。
- **报错文案只看字面**：区分「渲染给用户」与「内部日志」；后端透传的 err.message 只要会渲染就算发现，与长度无关。
- **不核实 subagent 输出**：对 🔴 级发现抽查读码确认，防止 subagent 误报或漏报。
- **改动未经确认**：默认只出表；用户点头后才动手，逐条保持全站口径一致。
- **把绝对路径/本机信息写进提示词**：发布型 skill 禁止硬编码路径（自己审计的 D 类错误不能自己犯）；特征库用内联全文或 `{SKILL_DIR}` 占位符传递，不写 `/Users/xxx`、`C:\Users\xxx` 之类。

## Verification Checklist（收尾自查）

- [ ] 对账表每条含：位置（文件:行号）+ 原文 + 分类 + 问题 + 建议 + 优先级
- [ ] 所有用户可见报错已按「发生了什么 + 用户能做什么」核查过
- [ ] 每条发现确认用户可见（排除测试/构建/内部日志）
- [ ] mock/示例数据已核实渲染路径（环境开关、store 初始态、SSG）
- [ ] 主 agent 只汇总未逐文件读文案（核实存疑项除外）
- [ ] 已交付对账表，未擅自改代码
- [ ] （如执行了修改）改完重跑相关 grep 确认无残留
- [ ] （如要发布）全文无绝对路径、无本机用户名/环境特定信息，特征库引用方式可移植

## 参考

- 特征库与典型例句：`references/stiff-copy-patterns.md`（扫描时逐条核对）
