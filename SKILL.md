---
name: public-ready
description: 上线前文案对账：扫描网站/Web 应用里的用户可见文案，按五类产出对账表——A 不适合直接上线（占位/测试残留/内部信息外露）、B 用户不友好（技术词、公文腔、甩锅式报错）、C 与初衷不符（语气/口径矛盾）、D 硬编码（写死的 URL/账号/版本号/重复文案）、E AI 味（AI 生成的空词、模板腔、emoji 堆砌、张三李四假数据）。当用户有以下任一意图时使用：做「发布前准备」「上线前检查」，问「能上线吗」「可以发布了吗」「见得了人吗」；觉得文案「一眼 AI 写的」「AI 味太重」「像 AI 生成的」「生硬」「技术向」「像程序员写的」「像没做完」「读起来怪」；想「检查/审计/扫描/打磨」文案、提示语、报错信息；排查「占位」「测试数据残留」「硬编码」「写死的」内容；或提到「文案扫描」「文案审计」「对账表」「上线检查」「去 AI 味」。默认只出对账表，用户确认后才改码。
version: 1.1.0
author: jry21223
license: MIT
metadata:
  hermes:
    tags:
      - copy-audit
      - launch-readiness
      - ux-copy
      - content-audit
      - ai-slop
    related_skills:
      - code-review
---

# 文案上线对账扫描（Copy Launch Audit）

扫描用户可见文案，产出「哪里不适合上线」的对账表。**默认只出表、不改代码、不往用户仓库里写文件**；用户明确要求后才动手。

## 何时使用

用户的表达不一定是专业词，任何与「上线/发布就绪度」相关的说法都可能触发：

- 上线前检查网站/应用里的用户可见文案是否达标
- 用户说「发布前准备」「上线前检查」「能上线吗」「可以发布了吗」「这个页面见得了人吗」
- 用户觉得文案「一眼就是 AI 写的」「AI 味太重」「太模板了」「生硬」「技术向」「像程序员写的」「像没做完」「读起来怪」
- 用户想「检查/审计/扫描/打磨」文案、提示语、报错信息；或排查「占位」「测试数据残留」「硬编码」「写死的」内容
- 需要一份「哪里要改」的清单，而不是直接改代码
- 排查硬编码：写死的域名、测试账号、版本号、提示语、重复文案

## 核心原则

1. **先出对账表，不直接改代码**。对账表是交付物，修改是用户确认后的后续动作。同理，**不主动往用户仓库写任何文件**（含画像文件、报告文件），落盘前必须问一句。
2. **每条发现必须可溯源**：位置（文件:行号）+ 原文 + 分类 + 问题 + 建议 + 优先级，缺一不可。
3. **判断基于证据**：引用具体文件和原文；拿不准的标注「存疑」并说明推断依据，不瞎编。
4. **尊重产品初衷**：先弄清产品定位、目标用户、语气基调再判断「与初衷不符」；没有文档就向用户确认，问不到就写成带标注的假设，不要停在那里等。
5. **报错信息必须写清原因**：暴露给用户的报错至少包含「发生了什么 + 用户能做什么」，禁止**只**甩一句「请稍后重试」「系统繁忙」；后端透传的原始错误（HTTP 状态、异常堆栈、内部配置问题）必须转换成用户能理解的表述，不得直出。
6. **分类和优先级是两件事**。分类说的是「这是什么毛病」，优先级说的是「这有多急」。B 类也可能是上线阻塞（付款页一句看不懂的报错就能让人放弃付款）。两者分别判定，不要照着分类抄优先级。
7. **扫描分派 subagent 执行**：不要让大量文案塞进主上下文。主 agent 只负责定范围、发 subagent、跨模块比对、汇总对账表；逐文件读文案的工作全部下放给 subagent。当前 Agent 没有 subagent 能力时走第 2 步的「单 agent 降级路径」。

## 工作流程

### 第 1 步：建立项目画像（多维度、可纠错）

动手扫描前，先探索项目并建立**多维度画像**，作为全部分类（尤其 C 类与初衷不符）的判断基准：

1. **探索**：读 README / 领域文档 / CONTEXT 文档，浏览目标站点（首页/代表页面），收集画像素材
2. **起草画像**，覆盖以下维度（用一句话/短列表，保持简洁）：
   - **定位**：这是什么、谁发起、官方还是非官方、一句话使命
   - **受众**：谁使用（游客/注册用户/运营角色）、各角色看什么
   - **功能**：核心模块及其价值主张
   - **核心理念**：产品坚持什么、拒绝什么（如说人话、数据诚实、不冒充官方）
   - **关键步骤**：用户主要动线（登录→使用→付费/提交→结果）
   - **语气基调**：整体语气、面向各角色的语气差异（如管理端可稍正式）
   - **边界红线**：绝不能出现的内容（内部信息外露、示例冒充真实、技术词直出）
3. **一次性展示画像给用户确认**：简洁呈现，请用户纠错/补充；不要来回追问
4. **等不到回应时的兜底**：如果是自动化/CI/用户不在场，或用户没有回应，**不要停在这里等**。把画像原样当作「待确认假设」继续往下扫，并在最终报告最前面单列一节「本次扫描基于的假设（请纠正）」，逐条列出。C 类发现在这种情况下一律降一级优先级并标注「依赖未确认画像」。
5. **落盘要先问**：画像定稿后询问用户是否写入 `<项目根>/.agents/public-ready-profile.md`（项目已有 agent 文档目录则放其中）。用户同意才写；不同意就只在本次会话内使用。写入时在文件头部记录：生成日期、当时的 HEAD commit 短哈希、扫描范围。
6. **复用画像要先验期**：读到已有画像文件时，对比其中记录的 commit 与当前 HEAD。若相差超过 100 个提交或生成日期超过 30 天，提示用户「画像可能已过期，是否重新建立」，得到确认再复用。
7. **扫描时携带画像**：subagent 提示词中内联画像关键维度（定位/受众/理念/语气/红线），C 类判断以此为准

### 第 2 步：扫描用户可见文案

用户可见文案 = 最终渲染到页面上的字符串。扫这些来源：

- UI 模板与组件：`.html` `.jsx` `.tsx` `.vue` `.svelte` 里的文本节点、`title`、`placeholder`、`aria-label`、`alt`、按钮、toast/提示语、空状态
- 国际化文件：`i18n`、`locale`、`messages`、语言包 `.json`
- 后端错误信息中会透传到前端的文案
- 配置与常量：路由、菜单、footer、联系信息、版本号、环境配置
- 静态示例数据：写死的榜单、示例订单、演示卡片——确认是展示稿还是真实数据，防止「示例当真实」
- 营销面：Hero 标题、功能卡片、CTA 按钮、about 页、空状态鼓励语——AI 味的重灾区

#### 2a. 分派 subagent（默认路径）

按用户可见面拆成多个并行 subagent（如 web 端、管理端、移动端、共享 UI 包各一个；具体目录以目标项目实际结构为准）。

**规模上限**：单个 subagent 不超过约 60 个文件或一个功能模块，超了继续拆；单次扫描的 subagent 总数控制在 8 个以内。超大仓库不要求一次扫全，按曝光度排序优先扫「首页 / 登录注册 / 付费或提交 / 错误页与空状态 / 全站公共组件」，并在报告里明确写出**本次未覆盖的范围**。

每个 subagent 的提示词必须包含：

```
你是文案审计扫描员。
扫描范围：<目录>（只扫这个目录）。
产品初衷：<一句话，来自第 1 步>。
用户可见文案 = 最终渲染到页面上的字符串（文本节点、placeholder、aria-label、alt、按钮、toast、空状态、i18n 语言包、后端透传错误文案、写死的示例数据、Hero 与功能卡片等营销文案）。
判断标准：按下方特征库逐条核对，归入 A/B/C/D/E 五类；报错文案检查「发生了什么 + 用户能做什么」是否齐全。
项目画像（C 类「与初衷不符」的判断基准，主 agent 发送前内联填入）：
- 定位/受众/功能/核心理念/关键步骤/语气基调/边界红线（逐条一行）
grep 线索命中不代表有问题，只有用户可见的才算发现；基础设施/测试代码不算。

输出两块，缺一不可：

【一】命中项：每条一行，格式
位置(文件:行号) | 原文(截断 60 字) | 分类(可组合如 A+D) | 问题 | 建议 | 优先级(🔴/🟠/🟡/⚪)
优先级独立判定，不要照分类字母套：
🔴 泄露内部或敏感信息 / 假数据被当真实数据渲染 / 用户看不懂就走不下去 / 合规风险（冒充官方、虚假宣传）/ 页面渲染 undefined、报错堆栈
🟠 用户看得懂但会困惑或做错动作 / 报错缺「能做什么」/ 首页价值主张缺失
🟡 不影响理解，影响一致性与观感
⚪ 用户无感的维护性问题
只输出命中项，不要解释说明。

【二】全局指纹（供主 agent 做跨模块比对，无论有没有命中都要回）：
- 重复文案候选：本目录内出现 2 次及以上的用户可见字符串，格式「原文 | 次数 | 代表位置」，最多 20 条
- 称呼用词：本目录用了「您」还是「你」，以及对用户的称谓（同学/用户/客户/伙伴…），各给 1 个例句
- 语气样本：随机 3 条代表性用户可见文案原文
- 产品自称：本目录里产品称呼自己的说法（产品名、"本系统"、"我们"…），列全

[特征库：见下方“特征库的传递方式”]
```

**特征库的传递方式（发布型 skill 强制可移植，禁止写死绝对路径）**：

- **默认：内联全文**。主 agent 用相对路径读取与本 SKILL.md 同目录的 `references/stiff-copy-patterns.md`，把全文粘贴到提示词末尾，并把上面模板最后一行替换为「特征库内容如下：<全文>」。零路径依赖，任何安装位置都能用。
- **备选：让 subagent 自己读**。仅当 subagent 能访问文件系统时使用。把最后一行替换为「先读取 `{SKILL_DIR}/references/stiff-copy-patterns.md` 全文作为特征库」，其中 `{SKILL_DIR}` 由主 agent 在发送前替换为 skill 信封 `<directory>` 提供的实际目录。

两种方式二选一，替换掉同一行，不要同时出现。任何情况下都不得把 `/Users/xxx`、`C:\Users\xxx` 这类本机路径写进提示词。

#### 2b. 单 agent 降级路径（当前 Agent 不支持 subagent 时）

Cursor、Codex、Zed 等不少 Agent 没有 subagent 机制。此时按目录**顺序分批**扫描，效果接近：

1. 把扫描范围拆成和 2a 同样的批次（每批 ≤ 60 文件）
2. 每批扫完**立即产出该批的命中项 + 全局指纹**，写进一个临时的中间结果（会话内的笔记或用户同意后的临时文件）
3. 产出后主动丢弃该批原文，不要把整批文案留在上下文里继续往下扫
4. 全部批次完成后，再统一做第 3 步的跨模块比对和第 5 步的汇总

降级路径下上下文压力更大，宁可多拆几批。批次之间不要为了省事跳过指纹收集，否则第 3 步没有输入。

### 第 3 步：跨模块比对（主 agent 执行，不可省略）

有一类问题**单个 subagent 结构上看不见**：它只看自己那块目录，判断不了「全站」。这一步由主 agent 用各 subagent 回传的「全局指纹」来做：

- **重复文案**：把各目录的重复候选合并计数。全项目累计出现 ≥ 3 次的同一字符串记为 D 类发现，位置写代表性的 2–3 处 + 总次数
- **称呼不一致**：各目录的「您 / 你」和用户称谓放在一起比。出现混用即记为 C 类发现，指出哪几个模块不一致，并给出统一建议
- **语气不一致**：把各目录的语气样本并排读一遍，明显割裂（如前台卖萌、管理端公文腔、错误页又是另一种）记为 C 类
- **产品自称不一致**：产品名、简称、"本系统/我们" 混用记为 C 类
- **AI 味浓度**：如果多个模块的语气样本都命中特征库 E 类，不要拆成十几条零散发现，合并成一条「全站 AI 味」总结项，位置列代表性 3 处，并在报告里单独给一段改写基调建议

**没有这一步，重复文案和一致性两类规则等于没有执行。** subagent 回传的指纹缺失时，主 agent 可以自己跑 grep 统计重复字符串（这属于允许的例外），但不要因此改成逐文件通读文案。

### 第 4 步：分类打标与优先级判定

**分类（问题是什么性质，A–E，可组合）**

| 分类 | 含义 | 典型特征 |
|------|------|----------|
| A 不适合直接上线 | 未完成、占位、调试残留、暴露内部信息 | `TODO`、`lorem`、`测试`、`示例`、`XXX`、`localhost`、报错堆栈直出、`undefined` 渲染到页面 |
| B 用户不友好 | 技术词外露、生硬书面语/公文腔、甩锅式提示 | 「参数校验失败」「接口超时」「token 过期」「404」「系统繁忙请稍后重试」 |
| C 与初衷不符 | 语气/口径与产品定位矛盾、价值主张缺失、全站不一致 | 校园正式平台用「兄弟们冲鸭」；学生工具用「贵司予以回复」；一会儿「您」一会儿「你」 |
| D 硬编码项 | 写死的、应配置化/走 i18n 的内容 | 硬编码 URL/IP/端口、环境名、版本号、联系人、日期、全项目重复 ≥3 次的同一文案 |
| E AI 味 | 一眼看出是 AI 生成的模板腔与空词 | 「让 XX 更简单」「一站式」「赋能」「探索无限可能」、emoji 当项目符号、「不是 X，而是 Y」句式、张三李四假数据、「太棒了！」 |

一条发现可同时打多个分类，用 `+` 连接（如 `A+E`），主分类在前。空状态缺失、错误页未设计归入 A。

**优先级（这有多急，独立判定）**

| 优先级 | 判定依据（命中任一即是） |
|--------|--------------------------|
| 🔴 上线阻塞 | 泄露内部信息或敏感数据；假数据被当作真实数据渲染；用户看不懂就走不下去（阻断核心动线）；合规与法律风险（冒充官方、虚假承诺）；页面渲染 `undefined`、`[object Object]`、报错堆栈 |
| 🟠 上线前应改 | 用户看得懂但会困惑或做错动作；报错缺「用户能做什么」；首页/落地页价值主张缺失；付费、注册等关键路径上的 B 类或 E 类 |
| 🟡 建议改 | 不影响理解，影响一致性与观感：称呼混用、局部 AI 味、语气割裂 |
| ⚪ 可分批 | 用户无感的维护性问题：硬编码、重复文案、i18n 缺失 |

规则：**不要照着分类字母套优先级。** 组合分类取其中最高的那一级。C 类在画像未经用户确认时降一级。

### 第 5 步：产出对账表

**同类合并（必做）**：同一句原文或同一个问题在多处出现时合并为一条，位置栏列出全部位置；超过 5 处写「代表位置 3 处，共 N 处」。报告概述里同时写「合并前 X 条 / 合并后 Y 条」，两个数都要给，不要只报其中一个。

报告结构：

1. **本次扫描基于的假设**（仅当画像未经用户确认时出现，放最前面，请用户纠正）
2. **概述**：扫描范围、未覆盖范围、产品初衷、发现总数（合并前/后）、各分类计数、各优先级计数
3. **对账表**：Markdown 表格，按优先级从高到低排
4. **全站性发现**：来自第 3 步的一致性问题与 AI 味总结，单列一节
5. **优先级建议**：🔴 必须改 / 🟠 上线前应改 / 🟡 建议改 / ⚪ 可分批
6. **改法示例**：每类给 1–2 条具体改法示范

对账表格式：

```
| # | 分类 | 位置 | 原文 | 问题 | 建议 | 优先级 |
|---|------|------|------|------|------|--------|
```

- 位置写「项目相对路径:行号」，同一文件多处用 `:12-15` 或拆多行
- 原文照抄，代码字符串加反引号；长文案截断但保留关键句
- 「问题」写为什么不行（对用户/对上线意味着什么）
- 「建议」写具体改法方向；拿不准时给两个候选让用户选

**交付形态**：发现超过 15 条时，把完整报告写成 `public-ready-audit-<YYYY-MM-DD>.md`（写盘前问用户放哪，默认项目根），对话里只贴「概述 + 优先级最高的 10 条 + 全站性发现」。几十条表格直接贴在对话里没人读得完。15 条以内可以直接贴。

**一条都没发现时**：不要只回一句「没问题」，用户会以为跑挂了。给一份「通过」报告，含：实际扫描的目录与文件数、五类分别核对了什么、逐类写明未命中、以及 2–3 条「不算问题但可以更好」的观察（比如某个空状态可以更有引导性）。

### 第 6 步：收尾

- 点出最该先处理的 3 个高优先级项，问用户是否动手改
- 用户确认修改后：逐条改，保持全站语气口径一致；**改报错文案时补全「发生了什么 + 用户能做什么」**；改完重跑相关 grep 确认无残留
- 硬编码的敏感信息（密钥、测试账号）即使不在扫描范围，也要单独提醒用户

## Common Pitfalls（常见陷阱）

- **grep 命中 ≠ 发现**：线索只用于定位，必须读上下文确认用户可见；测试、CI、构建配置、服务端内部日志不算发现。
- **主 agent 亲自逐文件读文案**：违背 subagent 机制，会污染上下文影响分类判断；只允许读少量文件核实存疑项，以及第 3 步的 grep 统计。
- **跳过第 3 步跨模块比对**：重复文案计数和称呼/语气一致性是全局问题，分片扫描的 subagent 结构上看不见。省了这步，这两类规则等于写着好看。
- **照分类字母套优先级**：A 不等于 🔴，B 不等于 🟠。付款页一句「参数校验失败」是 B 类，但它是上线阻塞。
- **漏查 mock 渲染路径**：mock 数据即使有环境开关保护，也可能作为 store 初始态、SSG 静态页渲染给用户；要追数据流（store 初始值、generateStaticParams、fallback），不能只看开关。
- **把「诚实性文案」误判成问题**：「未使用示例数据填充」这类不伪造数据的说明是正确设计，问题只在表达技术化——标 B 而非 A。
- **把正常用户词当技术词**：「ID」「配置」「API」在很多产品语境里就是用户词（学号 ID、个人配置、开放 API 文档页）。特征库里区分了「硬技术词」和「看受众定」两档，别一刀切。
- **报错文案只看字面**：区分「渲染给用户」与「内部日志」；后端透传的 err.message 只要会渲染就算发现，与长度无关。
- **误伤合理的「请稍后再试」**：禁止的是**只**说「请稍后重试」。已经写清原因后再补一句「稍后再试」是正确写法，不要把自己给的改法方案又标成问题。
- **AI 味拆得太碎**：全站统一的模板腔是一个问题，不是四十个问题。合并成一条总结项 + 代表位置，再给基调建议。
- **不核实 subagent 输出**：对 🔴 级发现抽查读码确认，防止 subagent 误报或漏报。
- **改动未经确认**：默认只出表；用户点头后才动手，逐条保持全站口径一致。
- **不打招呼就往用户仓库写文件**：画像文件和报告文件都要先问。用户跑一个「只读审计」却发现多了个 git diff，很伤信任。
- **没建画像就开扫**：先探索项目并起草多维度画像；C 类判断无基准、扫错方向都浪费。
- **画像等不到确认就干等**：自动化场景没人回答，标成假设继续扫，别卡死。
- **复用过期画像**：产品改版后旧画像会把 C 类判断带偏，复用前对比 commit 和日期。
- **把绝对路径/本机信息写进提示词**：发布型 skill 禁止硬编码路径（自己审计的 D 类错误不能自己犯）。

## Verification Checklist（收尾自查）

- [ ] 项目画像已建立（多维度），已请用户确认；未确认的已标为假设并写进报告首节
- [ ] 画像落盘前征求过用户同意；复用旧画像前核对过 commit 与日期
- [ ] 对账表每条含：位置（文件:行号）+ 原文 + 分类 + 问题 + 建议 + 优先级
- [ ] 优先级是独立判定的，不是照分类字母套的
- [ ] 第 3 步跨模块比对已执行：重复文案已全局计数、称呼/语气/自称一致性已比对
- [ ] 同类发现已合并，报告里给了合并前/后两个数
- [ ] 所有用户可见报错已按「发生了什么 + 用户能做什么」核查过
- [ ] 每条发现确认用户可见（排除测试/构建/内部日志）
- [ ] mock/示例数据已核实渲染路径（环境开关、store 初始态、SSG）
- [ ] 主 agent 只汇总未逐文件读文案（核实存疑项、全局 grep 统计除外）
- [ ] 未覆盖的范围已在报告里写明
- [ ] 报告交付形态正确（>15 条写文件 + 对话给摘要；0 条给「通过」报告而非一句话）
- [ ] 已交付对账表，未擅自改代码、未擅自写文件
- [ ] （如执行了修改）改完重跑相关 grep 确认无残留
- [ ] （如要发布）全文无绝对路径、无本机用户名/环境特定信息，特征库引用方式可移植

## 参考

- 特征库与典型例句：`references/stiff-copy-patterns.md`（扫描时逐条核对）
