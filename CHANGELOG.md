# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-08-13

### 新增

- **E 类：AI 味**。新增第五个分类，覆盖 AI 生成文案的模板腔：万能句式（「让 XX 更简单」「一站式」「探索无限可能」）、首页三段式结构、emoji 项目符号、张三李四假数据与五星好评腔、泛化情绪词、README 腔窜进用户界面。特征库补充 E1–E6 六组特征、「换名测试 / 具体性测试 / 风格≠问题」三道判定门槛与改法方向。
- **第 3 步：跨模块比对**。subagent 除命中项外额外回传「全局指纹」（重复文案候选、称呼用词、语气样本、产品自称），由主 agent 合并判定。此前「同一文案重复 3 次以上」和「称呼/语气一致性」两类规则在分片扫描架构下无人能执行。
- **单 agent 降级路径**。为 Cursor、Codex、Zed 等无 subagent 机制的 Agent 提供顺序分批扫描方案，README 的「不绑定 Agent 实现」不再是空头承诺。
- **规模上限与覆盖声明**。单 subagent ≤ 60 文件、单次 ≤ 8 个 subagent；超大仓库按曝光度优先扫描，并在报告中写明未覆盖范围。
- **零发现时的「通过」报告**格式，避免用户误以为扫描失败。
- **同类合并规则**，报告中同时给出合并前 / 合并后两个数字。
- **交付形态规定**：>15 条写入 `public-ready-audit-<日期>.md`，对话内只给概述与 Top 10。
- CHANGELOG.md。

### 变更

- **分类与优先级解耦**。此前 A/B/C/D 与 🔴/🟠/🟡/⚪ 一一对应，优先级字段形同冗余，且组合分类（如 `A+D`）的优先级无规则。现在分类只表示问题性质，优先级按独立的判定表给出，组合分类取最高级。付款页的「参数校验失败」是 B 类但属于上线阻塞。
- **画像落盘改为先征询**。此前强制写入 `<项目根>/.agents/public-ready-profile.md`，与「默认不动你的代码」的承诺冲突。现在需用户同意后才写，并在文件头记录生成日期与 HEAD commit。
- **画像复用增加验期**：commit 相差超过 100 或超过 30 天时提示复核。
- **画像确认增加兜底**：自动化 / CI / 用户不在场时不再干等，标为「待确认假设」继续扫描，报告首节单列，C 类发现降一级。
- **B 类技术词分两档**：硬技术词（token、鉴权、参数校验失败……）出现即改；语境词（ID、配置、API、IP）按项目画像里的受众判断，减少误报。
- **修正「请稍后重试」的自相矛盾**。此前特征库既禁止该表述，又在改法示例里使用它。现在明确：禁止的是**只**说这一句，说清原因后再补「稍后再试」是正确写法。
- **subagent 提示词模板消歧**。此前模板正文写「见本提示词末尾特征库内容」，但备选方案是让 subagent 自行读取 `{SKILL_DIR}`，两者指向冲突。现在改为替换同一行的二选一方案。
- **AI 味合并策略**：全站统一的模板腔合并为一条总结项 + 代表位置，不拆成几十条灌满对账表。
- README 重写：分类对照表此前把四类痛点硬凑成四个代号（A 只写了 mock、B 扛两个、D 是空的），现改为按五类如实描述；案例更换为 AI 味原文→改写对照。

## [1.0.0] - 2026-08

首个公开版本。四类分类（A 不适合上线 / B 用户友好向 / C 与初衷不符 / D 硬编码）、项目画像机制、subagent 并行扫描、对账表交付。
